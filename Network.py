import numpy as np

class NeuralNet:

	def __init__(self,dims,activation):
		#nrinputs is size of the first layer
		self.nr_layers = len(dims)
		for i in range(0,len(nr_layers)-1):
			self.W[i] = np.random.random([size_layers[i+1],size_layers[i]])*0.01
			self.b[i] = np.zeros(size_layers[i+1],1)


	def SGD(self,train_X,train_Y,epochs=10):

		return

	def pass_forward(self,train_X,gradient_calc = True):


		for i in range(0,len(nr_layers)-1):


		return
